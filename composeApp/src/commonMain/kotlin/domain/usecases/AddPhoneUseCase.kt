package domain.usecases

import data.repositories.ClientRepository

/*class AddPhoneUseCase(private val repository: ClientRepository) {
    suspend operator fun invoke(clientId: Int, phone: String): Result<Unit> = repository.addPhoneToClient(clientId, phone)
}

 */